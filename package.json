{
  "name": "file-codec",
  "version": "1.0.0",
  "description": "AES-GCM 256 file encryption/decryption — client-side only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vite-plugin-pwa": "^0.20.0"
  }
}
